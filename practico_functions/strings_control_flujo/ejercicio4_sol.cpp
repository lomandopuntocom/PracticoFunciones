#include <iostream>
#include <string>
using namespace std;

// Function prototypes Here
int multi_digitos*suma(int x, int y);
/**
 * @brief Recibe el valor de 2 numeros decimales, los suma y imprime su resultado
 * @Input  -
 * @Output -
 */

int main() {
	int n1, n2;
	getline (cin, n1, n2)
	cout<<"dime tus numeros"<<endl;
	cin>>n1>>n2
	multi_digitos*suma(n1, n2)
	return 0;
}

// Function definitions
multi_digitos*suma(x, y)
	int r, rm
	r = x + y;
	int size = 
	return result;
}
